{
  "name": "no-thanks",
  "version": "2.0.0",
  "description": "No Thanks! — Solo + Multijoueur réseau (Vue 3 + Socket.IO)",
  "type": "module",
  "scripts": {
    "dev":        "vite",
    "build":      "vite build",
    "preview":    "vite preview",
    "server":     "node server/index.cjs",
    "start":      "npm run build && npm run server",
    "dev:server": "node --watch server/index.cjs",
    "test:integration": "node tests/test-pause-disconnect.cjs"
  },
  "dependencies": {
    "socket.io-client": "^4.7.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "autoprefixer": "^10.4.0",
    "express": "^4.18.0",
    "postcss": "^8.4.0",
    "socket.io": "^4.7.0",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.0"
  }
}
